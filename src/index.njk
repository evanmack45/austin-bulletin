---
layout: layout.njk
title: "Today"
permalink: "/"
---
{% set latest = collections.bulletins | first %}
<article class="bulletin">
  {% set editionDate = latest.date %}
  {% set editionUrl = latest.url %}
  {% include "edition-head.njk" %}
  {% set glanceData = glance[editionDate | isoDate] %}
  {% include "glance.njk" %}
  {{ latest.templateContent | safe }}
  <p class="source-line"><a href="{{ editionUrl }}">Permanent link to this edition</a></p>
</article>
